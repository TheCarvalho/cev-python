
# *ex5 = Write a program that reads an integer number and displays its successor and predecessor on the screen.

num = int(input('Enter a integer number : '))
print('The predecessor is: {} \nand the successor is: {}'.format(num-1, num+1))
