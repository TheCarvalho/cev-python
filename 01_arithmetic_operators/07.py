
# *ex 7 = Develop a program that reads the two grades of a student, calculates and displays their average.

first_num = float(input('Enter the first exam grade: '))
second_num = float(input('Enter the second exam grade: '))


print('Your average grade is: {:.1f}'.format((first_num + second_num) / 2))
